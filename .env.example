# Optional API keys for bundled skills.
# Copy to your shell profile or a .env file loaded before running opencode.
#
# The pro agent uses a ChatGPT Plus subscription via `opencode auth` — not an API key.

# Brave Search skill (skills/brave-search)
BRAVE_API_KEY=
